# Database — direct PostgreSQL connection (NOT pooler) for migrations
# Supabase Dashboard → Settings → Database → Connection string (URI)
DATABASE_URI=postgresql://postgres:[YOUR-PASSWORD]@db.azjyukshkiedzjdagdth.supabase.co:5432/postgres

# Random secret — min 32 chars
# Generate: openssl rand -base64 48
PAYLOAD_SECRET=your-random-secret-here
